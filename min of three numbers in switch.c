//Finding min of three numbers
#include<stdio.h>
main()
{
	int a,b,c;
  printf("a:");
  scanf("%d",&a);
  printf("\nb:");
  scanf("\n%d",&b);
  printf("\nc:");
  scanf("\n%d",&c);
  switch(a<b && a<c)
  case 1:printf("\na is min");
  switch(b<a && b<c)
  case 1:printf("\nb is min");
  switch(c<a && c<b)
  case 1:printf("\nc is min");
}
  
  
  
  
