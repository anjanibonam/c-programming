#include<stdio.h>
main()
{
	int a,b,c;
  scanf("%d%d%d",&a,&b,&c);
  switch(a>b&& a>c)
  case 1:printf("\na is max");
  switch(b>a && b>c)
  case 1:printf("\nb is max");
  switch(c>a && c>b)
  case 1:printf("\nc is max");
}
